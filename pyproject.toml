[project]
name = "tictactoe"
version = "0.1.0"
description = "A simple Tic-Tac-Toe game."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "tqdm>=4.67.3",
]

[project.optional-dependencies]
dev = ["ipykernel>=7.1.0"]